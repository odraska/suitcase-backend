SilverStripe\GraphQL\Controller:
  cors:
    Enabled: true
    Allow-Origin: '*'
    Allow-Headers: 'Authorization, Content-Type, x-apollo-tracing, x-csrf-token'
    Allow-Methods: 'GET, POST, OPTIONS'
    Allow-Credentials: 'true'
    Max-Age: 600

SilverStripe\GraphQL\Schema\Schema:
  schemas:
    default:
      config:
        execute:
          - 'SLONline\App\GraphQL\AppSchema'

SLONline\App\GraphQL\CaptchaBotID:
  secret_key: 'z9F3k!q7@Lp%2V^x8*Gm#4R&h(0)-_+s=Tb?6Y|Q~cKJmN5%u'
